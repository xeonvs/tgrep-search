# tgrep Search Skill

A public, instruction-only skill for efficient and safe local source-tree search with [microsoft/tgrep](https://github.com/microsoft/tgrep).

The first public product release is prepared through a Draft pull request. Until that release is published, use only the canonical source and do not treat this repository as an installable marketplace package.
