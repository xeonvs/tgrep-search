# Agent Instructions

This repository publishes an instruction-only, public tgrep skill. Keep the canonical skill platform-neutral and free of private paths, credentials, local indexes, binaries, telemetry, hooks, MCP servers, and automatic installation behavior.

Use `PLANS.md` as active execution state. The canonical source is `skill/tgrep-search/`; the generated marketplace bundle is `plugins/tgrep-search/` and must be byte-identical in skill content. Validate package structure and public-content hygiene before a push or release.
